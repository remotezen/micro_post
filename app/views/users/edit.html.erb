<%provide(:button_text, 'Edit ' + @user.username + 'settings')%>
<div class="row">
	<div class="col-md-6 col-md-offset-3">
		<%= render 'users/form', button_title: "Update " + @user.username  %>
	</div>
	<div class="gravatar_edit">
		<%= gravatar_for @user %>
		<a href="http://gravatar.com/emails", target="_blank" rel="noopener">change</a>
	</div>
</div>
